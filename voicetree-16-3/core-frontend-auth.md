---
color: blue
isContextNode: false
agent_name: Ari
---
# Authentication

User auth with localStorage session

Login/register forms via `/.netlify/functions/auth`. Stores user + token in localStorage. Session restored on page load.

Handles auth modal, tabs, form submission, logout, profile/orders links.

[[netlify-functions]]
